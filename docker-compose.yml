version: '3.9'
services:
  db:
    image: postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USERNAME: postgres
      POSTGRES_PASSWORD: Teste3345#
      POSTGRES_DB: dscatalog
    networks:
      - reinan-network
  app:
     image: reinan07/dscatalog:latest
     restart: always
     environment:
       APP_PROFILE: prod
       DB_URL: jdbc:postgresql://db:5432/dscatalog
       DB_USERNAME: postgres
       DB_PASSWORD: Teste3345#
       SECRET_KEY: secretKey
     ports:
       - "80:80"
     depends_on:
       - db
     networks:
       - reinan-network

networks:
    reinan-network:
        driver: bridge

